#pragma once

// Copy this file to wifi_config.h and fill in your values.
// wifi_config.h is gitignored so it is not published.

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";
const char* udpAddress = "192.168.0.10";  // PC IP that receives UDP
const int udpPort = 50007;
